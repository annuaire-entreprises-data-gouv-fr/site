// This entry is only loaded in pages with react enabled.
// This is not the case for pages containing TVA or inpi pdf download

// In these page, we don't want to load sentry with the VITE config, 
// but use the config from next app

import './helpers-copy-paste';
import './helpers-feedbacks';
import './load-bar';

