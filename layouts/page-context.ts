import React from 'react';

// export const PageContext = React.createContext({ dirigeant: null });
export const PageContext = React.createContext({
  dirigeant: {
    name: 'Dubois',
    firstName: 'Angela',
  },
});
