describe('Check isProtectedSiren and isProtectedSiret', () => {
  test('Siren', async () => {
    // expect(await isProtectedSiren(verifySiren('552032534'))).toBe(false);
    // expect(await isProtectedSiren(verifySiren('907839872'))).toBe(true);
  });
  test('Siret', async () => {
    // expect(await isProtectedSiret(verifySiret('55203253400646'))).toBe(false);
    // expect(await isProtectedSiret(verifySiret('90783987200013'))).toBe(true);
  });
});

export {};
